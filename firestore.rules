
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access for all paths during development
    // WARNING: This is insecure for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
